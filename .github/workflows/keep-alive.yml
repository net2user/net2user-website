name: Keep Streamlit App Awake

on:
  schedule:
    # Runs every 12 hours
    - cron: '0 */12 * * *'
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping the Enterprise AI Adoption Advisor
        run: |
          curl -s -o /dev/null -w "%{http_code}" https://multi-agent-enterprise-ai-adoption-advisor-ixmdm7zxqmqrhxgkzna.streamlit.app/
